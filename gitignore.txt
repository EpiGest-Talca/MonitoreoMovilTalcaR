# renv: solo se versiona renv.lock y renv/activate.R, nunca la libreria de paquetes
renv/library/
renv/local/
renv/staging/
renv/sandbox/

# Archivos de sesion y proyecto de R
.Rhistory
.Rproj.user/
.Rapp.history

# Sistema operativo / sincronizacion Google Drive
.DS_Store
Thumbs.db
desktop.ini

# Nota: Data/, Figs/, Plots/ y Tables/ SI se versionan (requisito de la revista
# y para dejar trazabilidad de figuras/tablas del paper).
#
# Si algun archivo individual supera 100 MB (limite de GitHub), usar Git LFS:
#   git lfs install
#   git lfs track "Data/Raw/DPA_2023/**/*.shp"
#   git lfs track "Data/Raw/SHP_APC2023_R07/**/*.shp"
#   git add .gitattributes
